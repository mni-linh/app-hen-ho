import { styled } from "@mui/material/styles";
import AtomLink from "./AtomLink";

const AtomStyleLinkSoc = styled(AtomLink)({
  color: "#000000",
  ":hover": {
    color: "#d6002f",
  },
});

export default AtomStyleLinkSoc;
